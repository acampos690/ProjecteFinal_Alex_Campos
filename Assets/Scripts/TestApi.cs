using UnityEngine;

public class TestAPI : MonoBehaviour
{
    void Start()
    {/*
        // Buscamos el script de conexión en la escena
        APIConnection api = Object.FindFirstObjectByType<APIConnection>();

        if (api != null)
        {
            // Lanzamos la prueba
            StartCoroutine(api.PostHighScore("Alex_Unity", 777));
        }
        else
        {
            Debug.LogError("No he encontrado el APIManager en la escena.");
        }
        */
    }
}